# AI-SDLC v0.2 release readiness

This document is the durable release-readiness record for AI-SDLC `0.2.0`.

## Candidate status

`READY_FOR_MERGE`

Release-baseline PR #187 has completed its first exact-head validation pass successfully. Tag publication is still not permitted until the PR is merged and the exact merged `main` commit is rechecked.

## Release decision

The v0.2 required cross-repository lifecycle matrix is explicitly narrowed to the completed public external full-lifecycle dogfood plus the completed private repeated-dogfood target A lifecycle.

The security-sensitive private target B is deferred to post-v0.2 hardening. Its Feature remains active and is not marked DONE; no code, verification, acceptance, or release Gate is waived by this decision.

## Completed release evidence

- Feature-context dogfood: Issue #128 / PR #144 complete; final revision 10; workflow `DONE`; validation runs `31299038228` and `31299047295` passed.
- Private repeated-dogfood target A: final revision 7; workflow `DONE`; `code-gate: PASS`; `verification-gate: PASS`.
- Public external target: full standard-feature lifecycle completed through acceptance and release Gates.
- Target installation preflight: passed against two materially different private repositories.
- Repository publication safety: latest observed Public Release Audit run `31298771874` passed after post-public settings/protection changes.
- Public repository protection snapshot records `required-pr-gate`, pull-request enforcement, no force-push/delete, and full-length SHA Action policy.

## Final release-baseline PR evidence

PR #187 initial validated head: `450fc0ae61a2571e8301c91a92d8a1a657776866`.

Successful checks on that head:

- `Validate AI-SDLC protocol` — run `31300095581` — PASS. The `validate_release_readiness.py` step itself passed, as did the cross-repository control job.
- `Required PR Gate` — run `31300095640` — PASS.
- `Validate Public Runtime Distribution` — run `31300095603` — PASS.

The Public Release Audit workflow is path-scoped to publication-audit implementation/settings files. PR #187 does not change those scoped files, so it does not trigger a new Public Release Audit. The latest exact publication-safety audit remains run `31298771874`, which passed after the post-public settings snapshot changes.

This readiness-record update intentionally triggers the normal repository validation path again. The final PR head must remain green after this update before merge.

## Release-baseline contents

The release-baseline change includes:

- `VERSION` = `0.2.0`;
- `release/v0.2.0.yaml` with `status: release-candidate` and no known release blockers;
- finalized `release/v0.2.0-draft.yaml` with the explicit matrix-narrowing decision and deferred target B;
- version-aware `scripts/validate_release_readiness.py`;
- `CHANGELOG.md` section for `0.2.0`;
- README status aligned to the v0.2 baseline;
- this readiness record and the updated stabilization document.

## Required checks before tag publication

1. final PR #187 head remains green after this evidence update;
2. merge PR #187 through protected `main`;
3. confirm the exact `main` merge commit;
4. confirm `VERSION` and `release/v0.2.0.yaml` agree on that commit;
5. recheck the merged commit's repository validation/status evidence;
6. publish tag `v0.2.0` only from that reviewed merge commit;
7. create the GitHub release notes from the `CHANGELOG.md` v0.2 section.

## Deferred work

`F-PASSWORD-MANAGER-CRYPTO-COMPAT-0001` remains post-v0.2 hardening work. At the release decision point it is revision 9 with `implementation: WORKING`; `code-gate`, `verification-gate`, and `release-gate` remain pending.
