Public-release merged-branch cleanup trigger.

Revision: 3

Adding or changing this file on main intentionally runs the guarded merged-work-branch pruning workflow. The workflow deletes only short-lived branches whose current remote tip is already an ancestor of main.
