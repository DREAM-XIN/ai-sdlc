# Implementation Evidence — F-GHAW-DOMESTIC-PROVIDERS-0001

Feature: `F-GHAW-DOMESTIC-PROVIDERS-0001`

Issue: `#198`

PR: `#199`

Implementation payload candidate: `e519035ccec5d6dda924faab4ee0b8a538f10147`

## Outcome

The approved Design is implemented for Qwen, GLM, and MiniMax as trusted `experimental` OpenAI-compatible gh-aw profiles. The implementation keeps provider execution Registry-driven, preserves existing lifecycle/security authority, and does not claim live entitlement or bounded dogfood without separate provider evidence.

## WU-1 — Registry entries and bounded materialization mode

Implemented:

- added `qwen`, `glm`, and `minimax` to `runtimes/gh-aw/engine-profiles.yaml` with the approved endpoint/model/credential metadata;
- kept normal `load_registry()` source-existence validation strict;
- changed renderer write/materialization mode only to use `require_source_files=False` before newly registered generated sources exist;
- kept renderer `--check` and all normal consumers on strict source-existence validation;
- added `scripts/validate_gh_aw_worker_materialization.py` with positive and negative missing-source fixtures.

The Design Review MAJOR discovered before implementation was therefore closed in code rather than bypassed with hand-authored worker sources.

## WU-2 — Generated worker/profile surfaces

Generated through the deterministic renderer/materialization path:

- `.github/workflows/ai-sdlc-gh-aw-worker-qwen.md`
- `.github/workflows/ai-sdlc-gh-aw-worker-glm.md`
- `.github/workflows/ai-sdlc-gh-aw-worker-minimax.md`
- Registry-derived preflight profile choices;
- Registry-derived dispatch profile choices;
- boolean secret-presence plumbing for `DASHSCOPE_API_KEY`, `ZHIPUAI_API_KEY`, and `MINIMAX_API_KEY`.

Secret values are not serialized into Python arguments, JSON evidence, summaries, or Registry data.

## WU-3 — Open-ended legacy compatibility assertions

Changed test-only compatibility logic so the legacy five profiles remain protected without acting as Registry extension authority:

- `validate_gh_aw_engine_profiles.py` treats the legacy mappings as a required subset;
- `validate_gh_aw_runtime_preflight.py` protects legacy identity/maturity while deriving generic expectations from validated `EngineProfile` objects for all profiles;
- `validate_gh_aw_provider_registry.py` preserves the legacy/current cohort ordering prefix without requiring the Registry to remain an exact closed set.

During CI, a remaining exact five-profile ordering assertion was detected and removed. This was a test-only closed-set defect; runtime trust continues to come from the validated Registry.

## WU-4 — Registry-derived compile/materialization orchestration

`.github/workflows/compile-gh-aw-worker.yml` now:

- discovers the profile matrix from the fully validated Registry;
- resolves worker source/lock identity through the shared Registry helper;
- uses generic worker-source path filters;
- strict-compiles every registered profile with pinned `github/gh-aw v0.83.4`.

`.github/workflows/materialize-gh-aw-worker-lock.yml` now:

- uses bounded Registry loading only before source generation;
- immediately verifies generated sources through normal strict Registry loading;
- enumerates source/lock identities through the shared Registry helper rather than raw YAML parsing;
- generates Registry-derived profile surfaces;
- strict-compiles every registered worker.

## WU-5 — Compiler-generated lock artifacts

The three new locks were generated by the trusted materialization workflow on bounded branch `gh-aw/compile-domestic-providers` and transferred to the Feature branch through fast-forward ancestry; they were not hand-authored.

Compiler metadata includes:

- Qwen: schema `v4`, compiler `v0.83.4`, `strict: true`, `agent_id: copilot`, `agent_model: qwen3.7-plus`;
- GLM: schema `v4`, compiler `v0.83.4`, `strict: true`, `agent_id: copilot`, `agent_model: glm-5.2`;
- MiniMax: schema `v4`, compiler `v0.83.4`, `strict: true`, `agent_id: copilot`, `agent_model: MiniMax-M2.7`.

The PR compile matrix independently recompiles all registered profiles rather than trusting committed locks alone.

## WU-6 — Provider certification documentation

Updated:

- `docs/integrations/openai-compatible-providers.md`
- `docs/features/F-GHAW-DOMESTIC-PROVIDERS-0001/provider-certification.md`

The documentation records official provider source URLs and observation date `2026-08-09`, Qwen Beijing region/key coupling, direct GLM/MiniMax endpoint choices, and the distinction between static certification, live entitlement, bounded dogfood, and maturity.

Per-provider live status for this Feature:

- Qwen: live entitlement not established; bounded dogfood not established; `experimental`;
- GLM: live entitlement not established; bounded dogfood not established; `experimental`;
- MiniMax: live entitlement not established; bounded dogfood not established; `experimental`.

DeepSeek remains `experimental`; Kimi is not added.

## WU-7 — Integrated verification

Implementation payload candidate `e519035ccec5d6dda924faab4ee0b8a538f10147` passed all required PR workflows:

- Validate AI-SDLC protocol — run `31311150684` — **SUCCESS**;
- Validate Public Runtime Distribution — run `31311150688` — **SUCCESS**;
- Validate AI-SDLC gh-aw Worker Compile — run `31311150690` — **SUCCESS**;
- Required PR Gate — run `31311150764` — **SUCCESS**.

A preceding all-green code candidate `228f2d1c808e8ef15cf73ffaf5666100c886a0c6` also passed:

- Validate AI-SDLC protocol — `31311084817`;
- Validate Public Runtime Distribution — `31311084829`;
- Validate AI-SDLC gh-aw Worker Compile — `31311084812`;
- Required PR Gate — `31311084827`.

The Registry-derived compile run `31311084812` contained successful jobs for all eight profiles:

- `copilot`
- `codex`
- `claude`
- `gemini`
- `deepseek`
- `qwen`
- `glm`
- `minimax`

The protocol validator chain covers Registry validation, synthetic extension, bounded worker materialization, renderer/surface drift, gh-aw workflow security, effective-model metadata, command boundary, runtime preflight, lifecycle/persistence regression, and release-readiness checks.

## CI-driven corrections made during Implementation

Two closed-set assumptions were exposed by real CI and corrected without weakening trust:

1. `validate_action_security.py` recognized only the old provider lock filenames. It now derives exact trusted worker lock identities from the fully validated Registry and still requires pinned strict `v0.83.4`/schema-v4 compiler metadata before accepting generated lock credential behavior. Unregistered lookalike lock names fail closed.
2. `validate_gh_aw_provider_registry.py` required the current Registry to equal the original five-profile order. It now protects the reviewed legacy/current cohort prefix while allowing future reviewed profiles to append through the generic Registry contract.

## Security and authority boundary

Unchanged:

- no target Issue Comment provider/model/profile/credential/worker selectors;
- no direct provider HTTP calls in Commander, persistence, static preflight, or trusted routing;
- provider workers remain read-only by default and use Safe Outputs for GitHub writes;
- exact worker allowlisting is Registry-derived;
- Feature Manifest/Event/Gate authority is unchanged;
- Developer has not passed or waived `code-gate`;
- Developer has not performed independent Verification, Acceptance, merge, or release.

## Handoff to independent Code Review

Code Reviewer should explicitly verify the Design Review carry-forward item `DR2-MINOR-1`: `require_source_files=False` is structurally confined to deterministic source materialization/write bootstrap and cannot be selected by resolver, runtime preflight, effective-model audit, cross-repository allowlisting, target command inputs, or normal Registry reads.

Reviewer should also inspect the CI-driven `validate_action_security.py` generalization to ensure exact Registry worker identity plus pinned strict compiler attestation remains a sufficiently narrow exception for compiler-generated lock internals.
